{
  "name": "exif-image-js",
  "version": "0.1.3",
  "repository": "Akiya-Kudo/exif-image-js",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "build": "npm install && node_modules/.bin/tsc",
    "prepack": "npm run build",
    "test": "npm ls",
    "notusable-command": "npm run build && npm version patch && npm publish"
  },
  "author": "akidoki",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
